# Add How section

## data

Append to `${change_folder}/change.md` a `## How` section, see `@artdef_change_how`.

Rules:

- Do not start implementation, only add the `## How` section as described above
