# Add implementation plan section

## data

Inform the user the implementation plan is completed (?!constr_maybe)
Append the following sections to `${change_folder}/${impl_file}`: (?constr_maybe)
`@include_impl_sections`

Rules:

- Do not start implementation, only add sections as described above
