# Add implementation plan section

## data

Inform the user the implementation plan is completed (?!constr_maybe)
Append the following sections at the end of `${change_folder}/${impl_file}`: (?constr_maybe)
`@include_impl_sections`

Rules:

- Do not start implementation, only add sections at the end of the file as described above
