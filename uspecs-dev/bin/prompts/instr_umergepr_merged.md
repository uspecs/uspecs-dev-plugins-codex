# PR is in MERGED state

## data

PR #${pr_number} is in MERGED state. It has been opened in your browser.

Local branch `${branch_name}`, upstream tracking ref, and origin branch have been deleted (errors ignored).

To restore the local branch:

```text
git branch ${branch_name} ${branch_head}
```
