# Self-review: construction (Stage A)

## data

Review the construction artifacts that were just changed (or added) by `uimpl` for consistency with the change request.

Rules:

- For each issue found, fix it inline; do not stop to ask the user
- Do not re-run this stage

After fixing all issues, invoke the next stage:

- Run `bash "${softeng_sh}" self-review --type construction --stage B`
