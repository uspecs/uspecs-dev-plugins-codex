---
name: uspecs-upr
description: Create pull request from current branch
---

Parse user input as `[options]`.

run `bash ../../bin/softeng.sh action upr [options]` and follow the instructions in the output.

Options: `--no-archive`
