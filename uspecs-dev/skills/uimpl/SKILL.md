---
name: uspecs-uimpl
description: Implementation plan management
---

Parse user input as `[options]`.

run `bash ../../bin/softeng.sh action uimpl [options]` and follow the instructions in the output.

Options: `--change-folder <path>`
